// STUB — not yet called by the app. Mirrors createInvite() / acceptInvite()
// / getLinksForUser() in src/lib/api.js.
//
// import { neon } from '@netlify/neon'
//
// export default async (req) => {
//   const sql = neon()
//   // Route on req.method + a path segment or query param for
//   // create-invite / accept-invite / list, matching the three
//   // functions in api.js. Kept as one file for MVP; split apart
//   // if it grows unwieldy.
// }
